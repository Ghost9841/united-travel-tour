import React from 'react'

const TravelDetailPage = () => {
    // uselocalsearchparams here and then the folder has [id] 
    // so it will use it give it to ai it will do it
  return (
    <div>TravelDetailPage</div>
  )
}

export default TravelDetailPage